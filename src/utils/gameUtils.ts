/**
 * Game utility functions
 * Re-exports from gameLogic.ts for backward compatibility
 */

import { assignRoles, checkWinCondition, validatePlayerCount, validateRoleDistribution } from './gameLogic';

// Re-export functions
export { assignRoles, checkWinCondition, validatePlayerCount, validateRoleDistribution }; 